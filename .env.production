# 【生产】环境变量

# 应用部署基础路径（如部署在子目录 /admin，则设置为 /admin/）
VITE_BASE_URL = /

# API 地址前缀
# 若前后端同源部署（Nginx 反代 /api，或由 Go 后端托管静态资源），保持 / 即可
# 若前后端分离部署，改为完整后端地址，例如 http://192.168.1.100:8080
VITE_API_URL = /

# Delete console
VITE_DROP_CONSOLE = true